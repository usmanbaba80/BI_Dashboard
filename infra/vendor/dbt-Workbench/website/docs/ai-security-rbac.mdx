---
title: AI Security & RBAC - Secure AI for dbt Workflows
description: Security model and RBAC for dbt-Workbench AI copilot. Workspace isolation, encrypted secrets, proposal-based execution, and comprehensive audit logging.
slug: /ai-security-rbac
keywords: [dbt ai security, ai rbac, ai workspace isolation, encrypted ai secrets, ai audit logging]
seo_primary_keyword: "ai security rbac"
seo_intent: "informational"
last_reviewed: "2026-02-23"
---

import {BreadcrumbJsonLd, FaqJsonLd} from '@site/src/components/seo/JsonLd';

export const faqItems = [
  {
    question: 'Who can use AI features?',
    answer: 'Viewers, Developers, and Admins can all use AI chat features. Only Developers and Admins can confirm executable proposals.',
  },
  {
    question: 'How are AI secrets secured?',
    answer: 'API keys are encrypted at rest using AI_SECRETS_MASTER_KEY. Raw values are never returned by APIs.',
  },
  {
    question: 'Is AI data workspace-isolated?',
    answer: 'Yes. All AI entities including settings, secrets, conversations, and proposals are strictly workspace-scoped.',
  },
  {
    question: 'Are AI actions audited?',
    answer: 'Yes. All prompts, responses, tool traces, and proposal events are persisted for auditing.',
  },
];

<BreadcrumbJsonLd
  items={[
    {name: 'Docs', url: '/docs/'},
    {name: 'AI Security & RBAC'},
  ]}
/>
<FaqJsonLd items={faqItems} />

# AI Security & RBAC

## Access Model

- Viewer/Developer/Admin can use AI chat features.
- Only Developer/Admin can confirm executable proposals.

## Workspace Isolation

All AI entities are workspace-scoped:

- settings
- secrets
- MCP servers
- conversations/messages
- tool traces
- action proposals

## Secret Handling

- Secrets are encrypted at rest using `AI_SECRETS_MASTER_KEY`.
- Raw secret values are never returned by APIs.
- Environment variable fallback is supported.

## Proposal Confirmation

Executable SQL/dbt proposals are single-action records with status lifecycle:

- `pending`
- `executed`
- `rejected`
- `expired`

No proposal executes without explicit confirmation.

## Auditing

AI interactions persist:

- prompts/responses
- tool traces
- proposal creation and resolution events
